Write a function `countConstruct(target, wordBank)` that accepts  a targetString and an array of strings.

The function should return anumber of ways that the `target` can be constructed by concatenating elements of the `wordBank` array.

You may reuse elements of `wordBank` as many times as needed. 